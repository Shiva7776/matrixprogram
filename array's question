Write a C program to read N integers into an array A and to find the
(i)sum of odd numbers (ii) sum of even numbers
#include<stdio.h>
int main(){
    int i,esum=0,n=0,osum=0;
    printf("Enter the Size of the array \n");
    scanf("%d",&n);
    int a[n];
    printf("Enter the Element of the Array \n");
    for ( i = 0; i <n; i++)
    {
        /* code */
        scanf("%d",&a[i]);
    }
    for ( i = 0; i <n; i++)
    {
        if(a[i]%2==0){
            esum=esum+a[i];

        }else{
            osum=osum+a[i];
        }
    }
    printf("The Sum of the even number in array is \n %d\n",esum);
    printf("The Sum of thr Odd number in Arrary is \n %d\n",osum);
    
}
