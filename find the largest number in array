Write a program to find the largest number in array
#include<stdio.h>
int main(){
    int a[100],i,n,largest;
    printf("Enter the Actual Size of the array");
    scanf("%d",&n);
    printf("Enter the Element of the Array");
    for ( i = 0; i <n; i++)
    {
        scanf("%d",&a[i]);
    }
    largest=a[0];
    for ( i = 0; i <n; i++)
    {
    if (largest<a[i])
    {
        largest=a[i];
    }
    
    }
      printf("The Largest number is %d",largest);
  
}
