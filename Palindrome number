Write a progam for finding a number is Palindrom or not
#include<stdio.h>
int main(){
    int i,num,rem,temp=0,sum=0;
    printf("Enter the NUmber");
    scanf("%d",&num);
    temp=num;
    while (num>0)
    {
    rem=num%10;
    sum=sum*10+rem;
    num=num/10;
    }
    if (sum==temp)
    {
    printf("Number is palendrom");

    }else{
        printf("Not a palendrom no");
    }
    
    
    
}
